// // init db

// const app = express();
// app.use(cors())

// app.get('/', (req, res) => {
//     const news = db.prepare('SELECT * FROM news').all();
//     res.json(news);
// });

// initDb();

// app.listen(8080);
